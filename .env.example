# Postgres (Supabase, via Vercel Marketplace) — transaction pooler, required by Drizzle's postgres client
POSTGRES_URL=

# Supabase client (browser-safe) — used only to open the Realtime channel
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
